# Source-control / OS
.git
.gitignore
.DS_Store

# Local env (NEVER ship secrets into the image)
.env
.env.*
!.env.example

# Build artefacts (rebuilt inside the container)
dist
node_modules
*.log
npm-debug.log*

# Tests / dev tooling
coverage
.vscode
.idea

# Big docs that don't need to live in the image
MaintenanceWizard_PitchDeck.pdf
architecture.png
*.md
!README.md
tata-steel-maintenance-wizard-v8.html

# Optional secondary Python backend (built/deployed separately)
python-backend

# CI metadata
cloudbuild.yaml
deploy.sh
